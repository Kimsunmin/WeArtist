<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.we.art.user.model.repository.UserRepository">
	
	<select id="selectProPicByFIdx" resultType="fileVo">
		select * from tb_file
		where f_idx = #{fIdx}
	</select>
</mapper>